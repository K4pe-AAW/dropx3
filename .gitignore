# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts
tsconfig.tsbuildinfo

# production
/build

# env
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# data (contains generated drafts; keep articles.json but ignore local scratch)
/data/drafts.json.bak

# cron collect log (see README > ローカル自動実行について)
/log.txt

# raw source assets (ロゴ/背景の元データ。書き出し済みのpublic/images配下だけで運用に足りる)
/obj/
.vercel
.env*
